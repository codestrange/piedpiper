workdir = './data'
video_path = './example.mp4'

photos = workdir + '/photos'
videos = workdir + '/videos'

color_photos = photos + '/color'
gray_photos = photos + '/gray'

color_video = videos + '/color'
gray_video = videos + '/gray'

color_video_name = 'color'
gray_video_name = 'gray'
fft_video_name = 'fft'
wave_video_name = 'wave'

fft_photos_path = photos + '/fft'
fft_video_path = videos + '/fft'

fft_photos_umbral = fft_photos_path + '/umbral'
fft_photos_quantification = fft_photos_path + '/quant'

fft_video_umbral = fft_video_path + '/umbral'
fft_video_quantification = fft_video_path + '/quant'

wave_photos_path = photos + '/wave'
wave_video_path = videos + '/wave'

wave_photos_temp = wave_photos_path + '/temp'
wave_photos_esp = wave_photos_path + '/esp'

wave_video_temp = wave_photos_path + '/temp'
wave_video_esp = wave_photos_path + '/esp'
